services:
  - type: web
    name: chartink-fyers-trading-bot
    env: python
    buildCommand: "cd chartink-fyers-backend && pip install -r ../requirements.txt"
    startCommand: "cd chartink-fyers-backend && uvicorn main:app --host 0.0.0.0 --port 10000"
